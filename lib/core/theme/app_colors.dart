import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais do AppTheme
  static const Color spaceBlue = Color(
    0xFF1E293B,
  ); // Títulos e ícones principais
  static const Color mintGreen = Color(0xFF48BB78); // Botões de ação e sucesso
  static const Color emergencyRed = Color(0xFFC53030); // Card de emergência
  static const Color softGreyBg = Color(0xFFF7FAFC); // Fundo das telas
  static const Color cardWhite = Color(0xFFFFFFFF); // Fundo dos blocos/cards
  static const Color textMuted = Color(
    0xFF4A5568,
  ); // Subtítulos e textos secundários

  // Cores Adicionais Refatoradas (anteriormente hardcoded)
  static const Color tealPrimary = Color(
    0xFF009688,
  ); // Cor de destaque / Necessidades Visuais / Emergência
  static const Color darkBlue = Color(
    0xFF2C3E50,
  ); // Textos de seção / Títulos de perfil
  static const Color physicalNeedsGreen = Color(
    0xFF4CAF50,
  ); // Verde / Necessidades Físicas
  static const Color greyBg = Color(0xFFF8F9FA); // Fundo cinza alternativo
  static const Color switchGreen = Color(
    0xFF8BC34A,
  ); // Verde claro usado nos switches do perfil
  static const Color borderGrey = Color(
    0xFFE2E8F0,
  ); // Borda padrão dos containers
  static const Color limeGreen = Color(0xFF8DC63F);
  static const Color trackInactive = Color(0xFFFFB0BE);

  // Cores do Mapa e Legendados
  static const Color lightBlueBg = Color(0xFFEBF8FF);
  static const Color alertBlue = Color(0xFF3182CE);
  static const Color darkTeal = Color(0xFF319795);
  static const Color orangeAlert = Colors.orange;
}
